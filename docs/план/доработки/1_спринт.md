Принято. Дорабатываю **Sprint 1 (Auth)** минимальными патчами без ломки структуры и стиля. Цель: исправить «узкие места», выровнять контракт токенов, сделать `/auth/refresh` обратно-совместимым, добить покрытие тестами (в т.ч. ветки, которые у тебя сейчас «красные» в coverage).

---

# Что именно правим

1. **Единый контракт JWT**
   Добавляем claim `type` (`"access"`/`"refresh"`) + `iat`.
   `verify_access/verify_refresh` валидируют `iss` и `type`.

2. **/auth/refresh — совместимость тел**
   Эндпоинт принимает **и** «сырой JSON-строковый body», **и** `{ "refresh_token": "..." }`.
   Плюс: проверка истечения `expires_at`, ротация с `replaced_by`.

3. **Больше покрытие**
   Тесты на: повторную регистрацию, неверный логин, refresh со старым `jti`, refresh без токена, `/auth/me` с битым токеном, оба формата тела для `/auth/refresh`.

---

# Патчи кода (точечно)

## 1) `auth-svc/app/security.py` — контракты и валидация

```python
from __future__ import annotations
from datetime import datetime, timedelta, timezone
from pathlib import Path
from typing import Any
from jose import jwt
from passlib.context import CryptContext
from app.settings import settings

pwd_ctx = CryptContext(schemes=["bcrypt"], deprecated="auto")

PRIVATE_KEY = Path(settings.JWT_PRIVATE_KEY_PATH).read_text()
PUBLIC_KEY = Path(settings.JWT_PUBLIC_KEY_PATH).read_text()
ALGO = "RS256"

def hash_password(plain: str) -> str:
    return pwd_ctx.hash(plain)

def verify_password(plain: str, hashed: str) -> bool:
    return pwd_ctx.verify(plain, hashed)

def _now() -> datetime:
    return datetime.now(tz=timezone.utc)

def make_access_token(sub: str) -> str:
    exp = _now() + timedelta(seconds=settings.ACCESS_TTL)
    payload: dict[str, Any] = {
        "sub": sub,
        "iss": settings.JWT_ISS,
        "type": "access",
        "iat": int(_now().timestamp()),
        "exp": exp,
    }
    return jwt.encode(payload, PRIVATE_KEY, algorithm=ALGO)

def make_refresh_token(sub: str, jti: str) -> str:
    exp = _now() + timedelta(seconds=settings.REFRESH_TTL)
    payload: dict[str, Any] = {
        "sub": sub,
        "iss": settings.JWT_ISS,
        "type": "refresh",
        "jti": jti,
        "iat": int(_now().timestamp()),
        "exp": exp,
    }
    return jwt.encode(payload, PRIVATE_KEY, algorithm=ALGO)

def _decode(token: str) -> dict[str, Any]:
    # verify_aud=False — аудиторию не используем
    return jwt.decode(token, PUBLIC_KEY, algorithms=[ALGO], options={"verify_aud": False})

def verify_access(token: str) -> dict[str, Any]:
    payload = _decode(token)
    if payload.get("iss") != settings.JWT_ISS or payload.get("type") != "access":
        raise ValueError("invalid access token")
    return payload

def verify_refresh(token: str) -> dict[str, Any]:
    payload = _decode(token)
    if payload.get("iss") != settings.JWT_ISS or payload.get("type") != "refresh" or not payload.get("jti"):
        raise ValueError("invalid refresh token")
    return payload

def new_jti() -> str:
    import uuid
    return str(uuid.uuid4())
```

## 2) `auth-svc/app/deps.py` — без изменений интерфейса (валидация «type=access» уже внутри `verify_access`)

```python
from __future__ import annotations
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select
from app.db import get_session
from app.models import User
from app.security import verify_access

bearer = HTTPBearer(auto_error=True)

async def get_current_user(
    creds: HTTPAuthorizationCredentials = Depends(bearer),
    session: AsyncSession = Depends(get_session),
) -> User:
    try:
        payload = verify_access(creds.credentials)
        sub = payload.get("sub")
    except Exception:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="invalid token") from None

    user = (await session.execute(select(User).where(User.id == sub))).scalar_one_or_none()
    if not user:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="user not found")
    return user
```

## 3) `auth-svc/app/routers/auth.py` — совместимость тела `/refresh`, проверка `expires_at`, `replaced_by`

```python
from __future__ import annotations
from datetime import timedelta, datetime, timezone
from typing import Annotated, Any
from fastapi import APIRouter, Depends, HTTPException, Body, status
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select, update
from app.db import get_session
from app.models import User, RefreshToken
from app.schemes import RegisterIn, LoginIn, TokenPairOut, MeOut
from app.security import hash_password, verify_password, make_access_token, make_refresh_token, new_jti, verify_refresh
from app.deps import get_current_user
from app.settings import settings
import uuid

def utcnow() -> datetime:
    return datetime.now(tz=timezone.utc)

router = APIRouter(prefix="/auth", tags=["auth"])

@router.post("/register", status_code=201)
async def register(data: RegisterIn, session: Annotated[AsyncSession, Depends(get_session)]) -> TokenPairOut:
    exists = (await session.execute(select(User).where(User.email == data.email))).scalar_one_or_none()
    if exists:
        raise HTTPException(status_code=400, detail="email already registered")
    user = User(id=str(uuid.uuid4()), email=data.email, password_hash=hash_password(data.password))
    session.add(user)
    await session.flush()

    jti = new_jti()
    rt = RefreshToken(
        jti=jti, user_id=user.id, issued_at=utcnow(),
        expires_at=utcnow() + timedelta(seconds=settings.REFRESH_TTL),
        revoked=False
    )
    session.add(rt)
    await session.commit()

    return TokenPairOut(
        access_token=make_access_token(user.id),
        refresh_token=make_refresh_token(user.id, jti),
    )

@router.post("/login")
async def login(data: LoginIn, session: Annotated[AsyncSession, Depends(get_session)]) -> TokenPairOut:
    user = (await session.execute(select(User).where(User.email == data.email))).scalar_one_or_none()
    if not user or not verify_password(data.password, user.password_hash):
        raise HTTPException(status_code=401, detail="invalid credentials")
    jti = new_jti()
    rt = RefreshToken(
        jti=jti, user_id=user.id, issued_at=utcnow(),
        expires_at=utcnow() + timedelta(seconds=settings.REFRESH_TTL),
        revoked=False
    )
    session.add(rt)
    await session.commit()

    return TokenPairOut(
        access_token=make_access_token(user.id),
        refresh_token=make_refresh_token(user.id, jti),
    )

# Принимаем и сырую строку JSON, и {"refresh_token": "..."}
@router.post("/refresh", response_model=TokenPairOut)
async def refresh(
    data: Any = Body(...),
    session: Annotated[AsyncSession, Depends(get_session)] = Depends(get_session),
) -> TokenPairOut:
    # Извлекаем токен из разных форматов
    raw_token: str | None = None
    if isinstance(data, str):
        raw_token = data
    elif isinstance(data, dict):
        raw_token = data.get("refresh_token")
    if not raw_token:
        raise HTTPException(status_code=400, detail="refresh_token required")

    # Валидация refresh-JWT (iss/type/jti/exp)
    try:
        payload = verify_refresh(raw_token)
    except Exception:
        raise HTTPException(status_code=401, detail="invalid token") from None

    user_id = payload["sub"]
    jti_old = payload["jti"]

    rt = (
        await session.execute(
            select(RefreshToken).where(RefreshToken.jti == jti_old, RefreshToken.user_id == user_id)
        )
    ).scalar_one_or_none()
    if not rt or rt.revoked or rt.expires_at < utcnow():
        raise HTTPException(status_code=401, detail="refresh token revoked or expired")

    # Ротация: отозвать старый + создать новый
    jti_new = new_jti()
    await session.execute(
        update(RefreshToken)
        .where(RefreshToken.jti == jti_old)
        .values(revoked=True, replaced_by=jti_new)
    )
    new_rt = RefreshToken(
        jti=jti_new, user_id=user_id, issued_at=utcnow(),
        expires_at=utcnow() + timedelta(seconds=settings.REFRESH_TTL),
        revoked=False
    )
    session.add(new_rt)
    await session.commit()

    return TokenPairOut(
        access_token=make_access_token(user_id),
        refresh_token=make_refresh_token(user_id, jti_new),
    )

@router.get("/me", response_model=MeOut)
async def me(user: Annotated[User, Depends(get_current_user)]) -> MeOut:
    return MeOut(id=user.id, email=user.email)
```

> Совместимость с твоим старым тестом (где `/auth/refresh` получает «строку») **сохранена**.

---

## 4) Тесты — закрываем пробелы покрытия

### 4.1 `auth-svc/tests/test_auth_flow.py` (обновить/расширить)

```python
import pytest
from httpx import AsyncClient

@pytest.mark.anyio
async def test_register_login_me_refresh_both_payloads(client: AsyncClient):
    # регистрация
    r = await client.post("/auth/register", json={"email": "u@example.com", "password": "secret123"})
    assert r.status_code == 201, r.text
    data = r.json()
    assert "access_token" in data and "refresh_token" in data

    # повторная регистрация — 400
    r_dup = await client.post("/auth/register", json={"email": "u@example.com", "password": "secret123"})
    assert r_dup.status_code == 400

    # /me с access
    headers = {"Authorization": f"Bearer {data['access_token']}"}
    me = await client.get("/auth/me", headers=headers)
    assert me.status_code == 200 and me.json()["email"] == "u@example.com"

    # неверный логин — 401
    bad = await client.post("/auth/login", json={"email": "u@example.com", "password": "wrong"})
    assert bad.status_code == 401

    # валидный логин
    r2 = await client.post("/auth/login", json={"email": "u@example.com", "password": "secret123"})
    assert r2.status_code == 200
    d2 = r2.json()

    # refresh — формат «сырая строка»
    r3 = await client.post("/auth/refresh", json=d2["refresh_token"])
    assert r3.status_code == 200
    d3 = r3.json()
    assert d3["access_token"] and d3["refresh_token"]

    # старый refresh — должен быть отозван
    r4 = await client.post("/auth/refresh", json=d2["refresh_token"])
    assert r4.status_code == 401

    # refresh — формат {"refresh_token": "..."}
    r5 = await client.post("/auth/refresh", json={"refresh_token": d3["refresh_token"]})
    assert r5.status_code == 200

@pytest.mark.anyio
async def test_me_unauthorized(client: AsyncClient):
    r = await client.get("/auth/me")
    assert r.status_code == 401

@pytest.mark.anyio
async def test_refresh_requires_token(client: AsyncClient):
    r = await client.post("/auth/refresh", json={})
    assert r.status_code == 400
```

> Эти тесты закрывают ветки, которые сейчас у тебя «не покрыты» в `routers/auth.py` и поднимут общий coverage.

---

# Что сделать у тебя (пошагово)

1. Внести патчи выше в `auth-svc/app/security.py`, `auth-svc/app/deps.py`, `auth-svc/app/routers/auth.py`.
2. Обновить/добавить тесты в `auth-svc/tests/test_auth_flow.py`.
3. Убедиться, что ключи RSA на месте (как сейчас).
4. Запуск локально из `auth-svc/`:

   ```bash
   pip install -U pip && pip install .[dev]
   pytest -q --cov=app --cov-report=term-missing
   ```

   Цель: покрытие ядра ≥ 80% (должно стать выше прежних 82% и закрыть пропуски в роутере).
5. В docker-режиме ничего менять не нужно (контракты совместимы).

---

# Итог и дальнейшие шаги

* Sprint 1 закрыт по багфиксам: единый контракт токенов, совместимость `/refresh`, защита от просрочки/ревока, тест-покрытие веток-ошибок.
* Дальше (после того как прогонишь тесты) — скажи, если где-то «не бьётся»; добью точечно.
* Следующей итерацией (как планировали после Sprint 2) можно добавить **JWKS** и **kid-ротацию**, но это уже в отдельный спринт (я подготовлю минимальные патчи).
